<div class="pt-3">
	<%= form_with(model: [work, work.comments.build]) do |form| %>
		<% if work.errors.any? %>
			<div style="color: red">
				<h2><%= pluralize(work.errors.count, "error") %> prohibited this work from being saved:</h2>
				<ul>
					<% work.errors.each do |error| %>
						<li><%= error.full_message %></li>
					<% end %>
				<%end%>
			</ul>
		</div>
		<div class="mt-2">
			<%= form.text_area :body, class: "form-control " %>
		</div>
		<%if !parent.nil?%>
			<%=form.hidden_field :parent_id, value: parent.id%>
		<%end%>
		<div  class=" ">
			<%= form.button nil, type: "submit", class:'btn bg-transparent border border-0' do%>
				<i class= "fa-regular fa-comment fa-xl"></i>
			<%end%>
		</div>
	<% end %>
