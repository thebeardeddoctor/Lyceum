<%= form_with(model: work) do |form| %>
	<% if work.errors.any? %>
		<div style="color: red">
			<h2><%= pluralize(work.errors.count, "error") %> prohibited this work from being saved:</h2>
			<ul>
				<% work.errors.each do |error| %>
					<li><%= error.full_message %></li>
				<% end %>
			</ul>
		</div>
	<% end %>
	<div class="container mb-3">
		<%= form.label :title, class: "form-label" %>
		<%= form.text_field :title, class: "form-control"%>
	</div>
	<div class="container mb-3">
		<%= form.label :body, class: "form-label" %>
		<%= form.rich_text_area :body, class: "form-control vh-100" %>
	</div>
	<div style= "margin-left: 8rem"class=" position-absolute top-0 start-5 mt-3  ">
		<%= form.submit "create", class: "border-0 bg-transparent text-muted"%>
	</div>
<% end %>
