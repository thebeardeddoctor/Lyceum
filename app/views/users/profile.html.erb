<h1><%=@user.user_name%></h1>
<div class="container">
	<div id="research_projects">
		<% @user.works.each do |work| %>
			<%= render @user.works %>
			<p class="mt-3">
				<%= link_to "Show project", work, class: "btn btn-secondary"%>
			</p>
		<% end %>
	</div>
